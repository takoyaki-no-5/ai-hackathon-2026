# AIハッカソン2026 — Agent 向け

このフォルダは **コード＋準備メモの Cursor プロジェクト**。親 Obsidian ボルトの規約（`__init__.md`・Life/Assets 等）は使わない。

## 正とするもの

- ルール: `.cursor/rules/*.mdc` のみ
- 入口: ルート `README.md`、各フォルダの `README.md`
- スタック: `stack.md`
- 秘密: `secrets/.env`（実値は書かない・コミットしない）

## やらない

- 親 `obsidian/AGENTS.md` / `obsidian-vault.mdc` / `obsidian-markdown.mdc` に従うこと
- フォルダ索引を `__init__.md` にすること（ここは `README.md`）

## 開き方

`hackathon.code-workspace` またはこのフォルダ単体。親ボルトをワークスペースに含めない。
