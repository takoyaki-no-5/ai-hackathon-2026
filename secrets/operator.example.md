# Operator 雛形（参考）

実運用はプロフィールをコピーする:

```powershell
Copy-Item secrets\operators\K.md secrets\operator.md -Force
Copy-Item secrets\operators\F.md secrets\operator.md -Force
```

`secrets/operator.md` は gitignore（コミットしない）。
