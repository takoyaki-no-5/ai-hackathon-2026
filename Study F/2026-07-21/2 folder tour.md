### 2. フォルダの見方

枠: **〜15分**。**まだ文言は変えない**。

### 完了の定義

- [ ] `app/` を開いた
- [ ] `page` で始まるファイルを1つ開いた
- [ ] Cursor に「どこが画面の文字？」と聞いて説明できた
- [ ] 日本語の表示を1つ見つけた（直さない）

### 左の一覧で探す

| フォルダ／ファイル | 意味 |
|--------------------|------|
| `app/` | **画面の置き場** |
| `app/page.tsx` など | トップ画面 |
| `secrets/`・`.env` | **見ない・触らない** |

### Cursor に任せる

`app/page.tsx`（または似た名前）を開いた状態で `Cmd+L`:

```
このファイルのどこが、ブラウザに出ている文字ですか？
初心者向けに指差し説明して。まだコードは変えないで。
```

次: [[Study F/2026-07-21/3 try feature|今夜の機能を1回]]
