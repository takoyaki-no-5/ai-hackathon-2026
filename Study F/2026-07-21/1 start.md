### 1. 起動

枠: **5分**。毎回最初。

### 完了の定義

- [ ] `git pull` した
- [ ] Chrome で `http://localhost:3000` が見える

### コマンド

```bash
git pull
pnpm dev
```

### 自分で開く URL

- http://localhost:3000
- 公開 URL があればそれも可（K から）

### Cursor に任せる

```
git pull か pnpm dev でこのエラーが出た。初心者向けに直す手順をコマンド付きで。
.env は触らないで。
```

次: [[Study F/2026-07-21/2 repo read|リポを1ファイルずつ読む]]
