### 3. 文言を1か所だけ直す

枠: **〜15分**。**日本語の文字列だけ**。

### 完了の定義

- [ ] 1か所変えて、Chrome 再読込で確認した
- [ ] おかしければ `Cmd+Z` で戻した
- [ ] `git push` はしていない（基本）

### 手順

1. 直したい日本語を決める
2. `Cmd+Shift+F` でプロジェクト全体検索
3. **その文字列だけ** 変える（`import` など英語行は触らない）
4. `Cmd+S` → Chrome 再読込

### Cursor に任せる

```
この表示テキストだけ「○○」に変えたい。他の行は触らないで。
変えたあと localhost で確認する手順も書いて。git push はしないで。
```

次: [[Study F/2026-07-22/4 demo prep|デモ準備]]
