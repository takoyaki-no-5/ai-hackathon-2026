### R2c Agent厚く＋尖り1つ

枠: **20:45–22:00**（75分）。**22:00 機能追加打ち止め**。

本番: **60–150分**。

### 完了の定義

- [ ] R2a で選んだ **尖り1つ** が URL で動く
- [ ] Agent の tool 呼び出しが **画面上で追える**
- [ ] 22:00 以降は **新機能を足さない**

### 手順

#### 20:45 タイマー 75分（22:00 で実装停止）

#### 1. 尖りの接続（40分）

R2a で選んだ方のみ:

| 尖り | やること |
|------|----------|
| 画像入力 | #7 のフローをテーマ文言に合わせて調整。デモ画像 `public/demo/` を使う |
| 画像生成 | #8 の generate tool + 待ち UI。プロンプトをテーマに合わせる |

**新しい API や画面は増やさない。** 既存 route / page の文言・tool 説明の変更のみ。

#### 2. Agent を「厚く」（20分）

- tool を **あと1つ** 足すのは可（例: テーマ要約）。**新エンドポイントは不可**
- UI に「今: ○○ tool を実行中」を明示（審査の AI 活用度）

#### 3. デプロイ（10分）

```powershell
git commit -am "rehearsal: spike + agent"
git push
```

#### 4. 22:00 打ち止め

未完了があっても **ここで実装終了**。以降は R2d（デモ・逃げ）のみ。

### F 待ちで本線を止めない

F の質問はメモだけ。実装中は K が集中。

次: [[Guides/Prep/2026-07-23/R2d demo escape|R2d]]
